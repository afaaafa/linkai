<nav class="bg-white border-b border-gray-200 shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center" do %>
          <%= image_tag("tree.png", class: "h-8 w-8") %>
          <span class="ml-3 text-lg font-medium text-gray-800">Linkaí</span>
        <% end %>
      </div>
      <div class="flex items-center space-x-3">
        <% if !user_signed_in? %>
          <%= link_to "Sign up", new_user_registration_path, class: "text-white bg-indigo-500 hover:bg-indigo-600 font-medium rounded-md px-4 py-2 text-sm transition-colors duration-200" %>
          <%= link_to "Login", new_user_session_path, class: "text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-md px-4 py-2 text-sm transition-colors duration-200" %>
        <% else %>
          <%= link_to "Edit my account", edit_user_registration_path, class: "text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-md px-4 py-2 text-sm transition-colors duration-200" %>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "text-gray-600 bg-gray-100 hover:bg-gray-200 rounded-md px-4 py-2 text-sm transition-colors duration-200" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>